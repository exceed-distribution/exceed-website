{
  "name": "exceed-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Static landing page for Exceed Distribution, served on Railway.",
  "scripts": {
    "start": "serve -s . -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
